# 本地开发默认（可提交）。覆盖方式：在同目录创建 .env（已被 gitignore）
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/aidea
JWT_SECRET=development-jwt-secret-min-32-characters!!
JWT_EXPIRES_IN=24h
PORT=3000
BCRYPT_ROUNDS=12
